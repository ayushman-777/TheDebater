export enum UserRole {
  ADMIN,
  USER,
  EDITOR,
  NOUSER
}

export enum AuthenticationMode {
  OTP,
  EMAIL,
  GOOGLE,
  FACEBOOK,
  GUEST,
  NONE
}

export enum TopicName {
  WORLD,
  LOCALNEWS,
  TECHNOLOGY,
  BUSINESS,
  HEALTH
}
