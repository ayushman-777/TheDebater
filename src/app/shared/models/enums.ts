export enum UserRole {
  ADMIN,
  USER,
  EDITOR
}

export enum AuthenticationMode {
  OTP,
  EMAIL,
  GOOGLE,
  FACEBOOK,
  GUEST,
  NONE
}

export enum TopicName {
  WORLD,
  LOCALNEWS,
  TECHNOLOGY,
  BUSINESS,
  HEALTH
}
